using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AnimationSwitcher : MonoBehaviour
{
    /*
    public GameObject fullBodySprite = true;
    public GameObject separateParts = false;

    private Animator animator;

    void Start()
    {
        animator = GetComponent<Animator>();
    }

    void Update()
    {
        // Example of switching based on an animation parameter
        if (animator.GetCurrentAnimatorStateInfo(0).IsName("Fishing"))
        {
            fullBodySprite.SetActive(false);
            separateParts.SetActive(true);
        }
        else
        {
            fullBodySprite.SetActive(true);
            separateParts.SetActive(false);
        }
    }
    */
}

